Lexer:

Parser:
- when literals are done parse the expressions (without function calls by now, only literals, ops and identifiers)
- then parse var declaration statement
- then parse statement block
- then parse function definition