const GLOBAL_CONST: i32 = 100;

fn test_pointers(ptr: &i32) -> i32
{
	let a = *ptr;
	*ptr = a + 10;
	return *ptr;
}

fn test_loops_and_scopes(start: i32) -> i32
{
	let total = start;
	
	while total < 10
	{
		total = total + 1;

		if total == 5
		{
			let shadow = 100;
		}
	}

	for i in total..total + 3
	{
		let c = i * 2;
	}
	
	return total;
}

fn main() -> i32
{
	let a: i32 = -100;
	let b: bool = (a < GLOBAL_CONST) && true;

	if b
	{
		a = test_loops_and_scopes(a);
	}
	else
	{
		a = 404;
	}

	let final_val = test_pointers(&a);
	
	return final_val;
}